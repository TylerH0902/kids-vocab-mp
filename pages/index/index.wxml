<view class="page">

  <canvas
    id="planet-canvas"
    type="2d"
    class="planet-canvas"
    bindtouchstart="onTouchStart"
    catchtouchmove="onTouchMove"
    bindtouchend="onTouchEnd"
  ></canvas>

  <!-- CSS placeholder shown until canvas draws its first frame -->
  <view class="planet-loader" wx:if="{{!canvasReady}}">
    <view class="planet-loader-ball"></view>
  </view>

  <!-- Header floats over planet -->
  <view class="header-overlay">
    <text class="app-title">🌟 {{lang==='en'?'Story Quest':'故事大冒险'}}</text>
    <view class="lang-row">
      <view class="lang-btn {{lang==='en'?'active':''}}" bindtap="setLang" data-val="en">EN</view>
      <view class="lang-btn {{lang==='zh'?'active':''}}" bindtap="setLang" data-val="zh">中</view>
    </view>
  </view>

  <text class="hint {{hintVisible?'':'hint-hidden'}}">{{hintText}}</text>

</view>
